using System;

namespace NRobot.Selenium.Domain
{
    /// <summary>
    /// Location of where to launch the browser
    /// </summary>
    internal enum BrowserLocations
    {
        Local,
        Remote
    }
}
